# Go-Concurrent-Crawler

Concurrent web crawler using goroutines and semaphores for rate limiting.

## Quick Start

```bash
go test ./...
go run main.go
```

## Docker

```bash
docker build -t go-concurrent-crawler .
docker run go-concurrent-crawler
```
